---
import { getData, getGlobal, type PageData } from '@lib/api';
import PageRenderer from '@components/PageRenderer.astro';

const data = await getData<PageData>(`/error.json`);
const global = await getGlobal();
---

<PageRenderer slug="error" data={data} global={global} />
