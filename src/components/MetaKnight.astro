---
const { data } = Astro.props;
---

{
	data.global.meta
		.filter((meta) => meta.content)
		.map((meta, i) => {
			const { tag: Tag, html, ...rest } = meta;
			return <Tag {...rest}>{html}</Tag>;
		})
}
