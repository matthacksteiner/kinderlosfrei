---
const { title, font, size, align, color, Level } = Astro.props;
---

<section class={`block blockTitle`}>
	<Level class:list={[`font--${size}`, `text--${color}`, `text--${align}`]}>
		{title}
	</Level>
</section>

<style
	lang="css"
	define:vars={{
		font,
	}}
>
	.blockTitle {
		font-family: var(--font);
	}
</style>
